version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/query/"
    schema: "sql/migrations/"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
        emit_exact_table_names: false
